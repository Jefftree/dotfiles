timestamping = on

# 60 sec instead of default of 15 mins
timeout = 60

# Retry if rejected
retry_connrefused = on

# Add a `.html` extension to `text/html` files
adjust_extension = on

# HTTP server response
server_response = on
